# —— Ambiente e segredos (nunca commitar) ——
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Manter versionado: .env.example (template sem credenciais)

# —— Dependências e build ——
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo

# —— Logs ——
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log

# —— Testes / cobertura ——
coverage/
.nyc_output/

# —— SO e editores ——
.DS_Store
Thumbs.db
.idea/
*.swp
*.swo

# —— Design system local (DEV-only — referência extraída) ——
design-system/

# —— Documentação local (opcional; remover esta linha se quiseres versionar /docs) ——
docs/
